name: If Condition Test

on:
 push:
   branches:
     - main

jobs:
 test:
   runs-on: ubuntu-latest
   if: ${{ !startsWith(github.event.head_commit.message, 'chore:main') }}
   steps:
     - name: Echo result
       run: echo "Condition matched - starts with chore:main"
       
